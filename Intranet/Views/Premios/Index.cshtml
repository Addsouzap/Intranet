@model IEnumerable<Intranet.Models.Premio>

@{
    ViewBag.Title = "Prêmios";
}

<h2>Prêmios</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Indicados)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Laureados)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instituicao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PR)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Indicados)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Laureados)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Instituicao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PR)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                  @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@

                @Html.ActionLink("Details", "Details", new { id = item.Id }) 

            </td>
        </tr>
    }

</table>
